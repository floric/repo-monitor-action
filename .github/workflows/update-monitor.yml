name: Update Monitor

on:
  push:
    branches:
      - master

jobs:
  UpdateMonitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.1
      - name: Calculate lines of code
        run: |
          SIZE=($(du -s dist/))
          echo "::set-output name=code_size::$SIZE"
      - uses: floric/repo-monitor-action@v1.2.9
        name: Update Report
        with:
          key: code-size
          value: $OUTPUT_CODE_SIZE
          token: ${{ secrets.GITHUB_TOKEN }}
